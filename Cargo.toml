[package]
name = "algebraic"
version = "0.1.2"
edition = "2024"
description = "Ergonomic algebraic (auto-vectorizable) floating-point wrapper types for Rust"
license = "MPL-2.0"
repository = "https://github.com/tormeh/algebraic"
rust-version = "1.98"
keywords = ["simd", "numerics", "float"]
categories = ["hardware-support", "mathematics", "no-std"]

[dependencies]
serde = { version = "1.0.229", optional = true, default-features = false }

[features]
f16 = []
f128 = []
serde = ["dep:serde"]

[lints.clippy]
all = "deny"
pedantic = "deny"
nursery = "deny"
indexing_slicing = "deny"

[dev-dependencies]
criterion = { version = "0.8.2", default-features = false, features = ["html_reports"] }
serde_test = "1.0.177"

[[bench]]
name = "arithmetic"
harness = false
